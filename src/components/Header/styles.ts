import themes from "@themes/index"
import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container:{
        backgroundColor: themes.base.gray700,
        height: 173,
        justifyContent: "center",
        alignItems: "center"
    }
})